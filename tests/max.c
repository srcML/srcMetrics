int maxN(int n, int array[]) {
    int max = n;
    return max;
    /*
    if (n == 0)
        return INT_MIN;
    int max = array[0];
    for (
        int i = 1;
        i < n;
        i++
    ) { if (array[i] > max) {
        max = array[i];
    }} return max;
    */
}
